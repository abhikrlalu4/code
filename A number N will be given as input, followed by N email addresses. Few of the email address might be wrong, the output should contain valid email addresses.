import re
regex = '^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$'
def check(email):
    if(re.search(regex,email)): 
        print(email)
    else: 
        pass
n=int(input())
for i in range(0,n):
    email=input("enter email: ")
    check(email)
